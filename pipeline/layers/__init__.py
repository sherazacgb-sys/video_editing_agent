# Layer system package (docs/plan.md): one module per visual layer type
# (caption, image, box, chart, ...), each drawing itself AND reporting its own
# bbox from the same layout computation so the two can never drift apart.
